require('dotenv').config(); const { GoogleGenerativeAI } = require('@google/generative-ai'); async function testGemini() { const key = process.env.GEMINI_API_KEY; console.log('--- Gemini Diagnostic Tool ---'); console.log('API Key found:', key ? 'YES (Starts with ' + key.substring(0, 4) + ')' : 'NO'); if (!key) { console.error(' ERROR: GEMINI_API_KEY not found in .env'); return; } try { const genAI = new GoogleGenerativeAI(key); const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' }); const result = await model.generateContent('Respond OK if you see this.'); const response = await result.response; console.log(' SUCCESS:', response.text().trim()); } catch (err) { console.error(' FAILED:', err.message); } } testGemini();